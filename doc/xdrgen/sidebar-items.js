initSidebarItems({"fn":[["compile","Simplest possible way to generate Rust code from an XDR specification.It is intended for use in a build.rs script:Output is put into OUT_DIR, and can be included:If your specification uses types which are not within the specification, you can provide your own implementations of `Pack` and `Unpack` for them."],["generate","Generate Rust code from an RFC4506 XDR specification`infile` is simply a string used in error messages; it may be empty. `input` is a read stream of the specification, and `output` is where the generated code is sent."]]});